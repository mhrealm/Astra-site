<section class="listing-hero">
  <p class="eyebrow">Writing</p>
  <h1>博客文章</h1>
  <p>按主题整理前端实践笔记，快速找到性能、动画、Vue 和业务编码相关内容。</p>
</section>

<style>
  .listing-hero {
    margin: 0 auto 2rem;
    max-width: 720px;
    text-align: center;
  }

  .eyebrow {
    color: var(--accent);
    font-size: 0.84rem;
    font-weight: 800;
    letter-spacing: 0;
    margin: 0 0 0.85rem;
    text-transform: uppercase;
  }

  .listing-hero h1 {
    margin-bottom: 0.85rem;
  }

  .listing-hero p:not(.eyebrow) {
    color: var(--muted);
    margin: 0;
  }
</style>
